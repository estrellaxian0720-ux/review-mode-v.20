为「云记学习模式」生成「全屏知识地图」沉浸页。从 Overview 的星图预览卡进入。顶部一个返回按钮 + 标题「知识地图」。

【顶部 chrome 带（一条收敛，星海才干净）】所有控件收在顶部一条水平带里，中部到底部整片星海不放任何常驻控件（除章节水印外）：
  · 左端：返回按钮 + 标题「知识地图」。
  · 左侧接着排**筛选 chip**（见下）。
  · 右端：三视图分段控件 [★ 星图] [思维导图] [列表]，默认选「星图」。三视图共享同一份数据与同一套筛选 chip。
  · 平板可达性：顶部带做足高度（约 56–64px），chip 用大点击区药丸胶囊、间距充足，横握拇指也好按——不要因为"平板下方好按"就把 chip 挪到画布下方浮层（那会压住星海与章节竖字、破坏"星是主角"，正是要避免的散乱）。
  · 备注（未来若真出现顶部够不着的可达性反馈再考虑下方浮层，前提必须同时满足）：≤4 个 chip、极轻量（不选中不描边、透明底）、绝不压在章节水印竖字上。当前版本一律用左上角顶部带，不做下方浮层。

【筛选 chip】顶部带左侧一行筛选标签，**只有 4 个：全部 / 待复习 / 薄弱 / 已掌握**。定位是"看全貌"下的高价值筛选，不要堆砌重复维度。
  · 这 4 个各有不可替代的动作语义，不许合并：全部=基线；已掌握=看点亮成就；**薄弱**=连错达阈值的"真不会"（全图唯一警示红星，单独隔离出来价值最高）；**待复习**=到期记忆衰退的"学过快忘了"（去快速复习）。注意「薄弱」与「待复习」是两种不同的病、对应"去强化 vs 去复习"两个不同动作，**不能合成一个 chip**。
  · **不放「今日待学」常驻 chip**：Overview 的用户心智是看全貌 / 看积累，"今天学什么"是 Today 页的事、已在首页强化过。今日待学仅作为从 Today「查看全部 →」进入时的**临时高亮态**（进入即高亮今日那批星并可清除），不在本页 chip 行常驻占位。
  · **不放「收藏」chip**：收藏走页头 `☆ 收藏N` 入口按收藏筛选，不在地图 chip 里重复。
  · 也不要再拆出「练习中」「未学」等 chip——它们由星的亮度已表达，无需筛选维度。

【新手引导：悬浮气泡 bubble（递进 + 用完即撤，同 Today 机制）】进入本页时给两个悬浮气泡（带 💡 图标 + 关闭 ✕ + 指向热区的小尾巴，浮在界面上、不占布局行高），做过对应操作即永久撤下（按次数、跨会话），一次只浮一个、前一条完成后才出现下一条；产出「有气泡/无气泡」两种状态示意，不要画成 toggle。
  · 气泡①（切视图）：指向 [★星图][思维导图][列表] 分段控件，文案「换个看法：星图看全貌、列表快速查」。用户切换过一次视图即撤。
  · 气泡②（切范围）：首次切过视图后才出现，指向筛选 chip 那一行，文案「点这里筛选查看范围：只看薄弱 / 已掌握」。用过一次筛选即撤。

—— 视图 A：星图（默认，核心，要做出「点亮地图」的体感）——
深色夜空背景。这是一张可缩放、可平移、有动画的地图（交互体感类比谷歌地图），不是一张静态星空插画。

★ 第一原则：星是主角，文字按需出现。
  上一版糊掉的根因是「小节名 + 英文副标 + 章节水印 + 知识点名」四层文字同时平铺，抢光视线，星反成背景小点。本版必须倒过来：
  - 任何时刻屏幕上的常驻文字只剩「章节名」一层（极淡大字水印，像地图国家名，作定向锚点）。
  - 知识点名默认全部隐藏，只在三种时刻淡入浮出：① 点击该星；② fly-to 聚焦后的当前簇；③ 命中筛选 chip 的那批。其余时刻只有星、没有名。
  - 小节退为簇中心一个小标记点 / 极轻标签，不抢视线。
  - ‼【全屏星图不挂常驻成就徽章 / HUD（与 Overview 预览的区别）】Overview 的星图预览是"成就墙"、允许悬浮成就徽章群组（连续/累计/已点亮N颗）；但**全屏星图这里星是主角，不得挂那套常驻成就 HUD 徽章**。全屏只在顶部带一行轻量统计（如「41% 已亮 · 本周新点亮 8 颗」），成就叙事靠星本身的点亮/光晕表达，不靠悬浮徽章卡。别把 Overview 预览的徽章浮层照搬进来。
  - 删除一切英文副标（如 CONSTELLATION AREA）——中文界面，不要装饰性英文。

★ 节点（星）视觉权重——星要够突出、要"闪耀"，且只用 2 个色相：
  - 配色收敛（关键，别五颜六色）：星图核心不是"用什么颜色"，而是**深邃 vs 明亮的对比**——**深邃的夜空底 + 明亮闪耀的星**，符合星空的一般理解。星只用 1 个暖白偏冷的**发光色相**做主角（趋近星光白 / 极浅金白，落在深底上自然发亮即可，不必对齐品牌色板），配未学的中性暗灰。已开始之后的练习中/待复习/已掌握**不用不同色相区分，只用大小·亮度·深浅**：已掌握=最亮最大带 bloom 光晕（成片发光）、练习中=中等亮、待复习=压暗压深。未学=最暗、仅轮廓微光。薄弱可保留极少量警示红 #FF6252 作唯一例外。整体追求"闪耀的星海"，不要红黄蓝绿灰糖果色噪点，也不必纠结星色是蓝是黄——**融合成一片会发光的星空**即可。
  - 三通道编码（都在同色相内）：status(亮度) × 数据里每颗星的 deg 枢纽度(大小) × 是否点亮(光晕有无)。deg 越大的星显著越大 → 造"主角大星"。
  - 已掌握星带 bloom 光晕 + additive 加色混合：成簇的已掌握星融成一片发光星云，这是"闪耀"的核心。未学星最小最暗、仅轮廓。
  - 被点击/聚焦的星再放大一档 + 描边高亮，与周围拉开层级。

【背景星尘层（"满天星"的关键，必须加）】在真实知识点星之下铺一层 300~600 颗极小极暗的装饰性背景星（随机分布、可极缓慢闪烁/漂移）：不可点、不计进度、不属于任何知识点，纯粹负责深空质感 + 填满簇间暗空。没有这层画面永远显空。

三层模型（地图层级）：章节（chapter，最高分组，作定向锚点）→ 小节（section）→ 知识点（concept = 一颗星，地图叶子节点）。
  星 = 知识点。点一颗星打开该知识点，默认以闪卡形式展示（闪卡只是知识点放在卡片上的展示形式，还可列表/练习题）。

【语义缩放 / LOD（最关键，放大=更细）】当前缩放比例决定显示到哪一层、谁是主角。严禁任何档位把所有层级的标签同时全字号平铺（会糊成一团、看不见星）：
  - 缩小（全局档）：只显示「章节」大字标签（淡、置底，浮在星云背景上、不圈定区域）；知识点收成微光点簇，不显名。
  - 中等档：浮现「小节」小标记；知识点显形为小亮点，仍不显名。
  - 放大（细节档）：「知识点」成为主角，星大而亮、可点；名字仍默认不显，聚焦/点击才浮出；章节名退为极淡大字背景锚点。
  字号分层铁律：章节名=淡/大/置底，知识点名=小/亮/置顶且按需出现。跨档缩放时层级标签平滑淡入淡出。

【动画过渡（必须做，不能是静态图）】
  - 进入：fly-to 到「今日学习」簇，带 ease-out 缓动，不硬切。
  - 跨档缩放：章节↔小节↔知识点名 标签平滑淡入淡出，不闪现、不跳变。
  - 点亮：学会一颗星 → 由暗到亮的脉冲 + 光晕扩散一下再收住。
  - 连线：相关连线从已亮星「生长」到目标星（draw-on 描线，非瞬现）。
  - 点击标签：知识点名 fade-in + 轻微上移浮出，点别处淡出。
  - 待机：已掌握星极轻微呼吸/闪烁，让夜空"活着"，克制、不晃眼。

【章节锚点常驻】即使放大到知识点档，章节名仍以极淡大字浮在背景（像地图上的国家名），告诉用户「我在哪片天区」，纯星空才不会迷路。这是唯一的常驻文字层。注意：章节名只是飘在星云上的一行淡字、落在该章节星群的大致中心，**不是一个圈起来的区域、不画边界、不把星按章节聚成块**——星仍是打散交错的一片。

【进入与交互（类比谷歌地图）】
  - 进入星图 → 自动 fly-to 到「今日学习」所在的小节/知识点簇并适度放大，不停在全局。
  - 点任意节点 → fly-to + 居中并放大该节点及周边，看清局部关系（主探索手势）。
  - 双指缩放/拖拽平移贯穿全程；点章节锚点 → 缩放回该章节全貌。

【点星先确认、再进卡（241 星密度，防点错）】点一颗星不直接全屏闪卡：
  - 点星 → 该星放大高亮 + fly-to 居中 + 浮出一个小预览气泡，气泡内显示完整知识点名 + 状态标签。
  - 气泡上挂两个动作按钮：「看闪卡」（打开全屏预览闪卡）/「去练习」（从这一个知识点拉起练习）。
  - 点气泡外空白即关闭，点错零成本，不会被拽进任何卡/练习。

【筛选 chip 命中态：变暗不隐藏】选某个 chip（如薄弱）时，命中的星高亮浮出名字，非命中的星压暗但保留在原位（不隐藏），守住地图骨架与方位感。连线同理：只点亮命中星相关连线，其余淡化不删。

【渐进点亮 + 连线（不实时算关系）】
  - 整张图谱（节点坐标 + 邻接边）离线算好、固定不变；初始全暗、连线隐藏。
  - 学会一个知识点 → 这颗星亮起（亮起动画）+ 向它的 top-3 相关邻居画出连线（draw-on 生长，邻居即使没学也连）。
  - 连线显示条件 = 任一端掌握即显（非两端都掌握）。
  - 「亮星牵出一条线指向暗星」= 推荐的下一个该学的，用户顺线按图索骥找下一步。

【未学暗区 = 待探索，不是被惩罚】暗区（如刚起步的总论，整片暗星）要用探索叙事，不用欠账口径：
  - 未学星画成「还没点亮的星」（有轮廓 + 微光底），不要画成灰叉/空缺/报错。
  - 文案正向：章节锚点旁挂「这片星域等你点亮 ✦ · 共 N 颗」；说「已点亮 8 颗」而非「还差 95 颗」。
  - 几乎全暗时给整图一句总引导：「你的刑法星图才刚开始，点亮第一颗 →」。

【连线显示规则（防毛球）】仅在「知识点档」显示连线；章节/小节档不显示连线。连线只在当前聚焦簇内点亮，远处淡化。

【星的亮度 = 掌握状态（星空亮度制）】不靠色相区分，靠**深邃底上的亮度·大小·深浅**：未学=中性暗灰仅轮廓、练习中=中等亮、待复习=压暗、已掌握=最亮最大带 bloom 光晕。星本身是暖白/星光白的发光体（不必对齐品牌色板），薄弱=极少量警示红为唯一例外。追求闪耀星海，不要五种糖果色相。
小节内知识点全掌握 → 该星座连线整体点亮高亮（小庆祝）。

【布局（核心，决定星图像不像满天星）】
  ‼ 给生成器的直接渲染指令（最重要，先读这条）：
  每颗星的位置只看数据里的 `x` / `y`（0~1 归一化坐标），**直接按 x,y 画点**，大小按 `deg` 字段（枢纽度，越大越大）。不要自己重新布局、不要按章节分块。
  - 坐标已是「成簇 + 暗空」形态：多个紧致星簇挤在画面中部、簇间和四周留大片暗空（由背景星尘填底）。**这是特意的密度对比，不要把它们均匀摊开填满画布**。
  - 不存在「章节区域」「章节圆盘」「左右分区」——章节只是背景上一行极淡大字，落在其星群大致中心，不圈区域。
  - 反例（禁止）：① 按章节摆成左右两团；② 把星均匀平铺铺满整屏（=处处稀疏、不震撼）。正解=密簇 + 暗空 + 背景星尘。

  坐标怎么来的（真实数据离线算，生成器不必自己算，见《星图布局规则.md》）：
  1. 每个知识点算 embedding → cosine 相似度 → kNN（无向并集、低阈值过滤）。
  2. 边数双轨：布局图 k=8 求稳坐标；显示图每点 top-3 写进 relations（画线 + 学习路径）。
  3. force-directed，**成簇取向**：强吸引 + 降斥力 + 相似度锐化 → 相关星收成紧致核心、簇间留暗空（不是均匀平铺）。
  4. 每颗星记录 `deg`（显示边数）供渲染放大枢纽星；算完把 x,y、deg 固化写进数据。生成器只按数据画。
  可视验收目标：约 241 颗知识点星聚成几个发光密簇 + 大片暗空，暗空里填满数百颗极暗背景星尘，已掌握星带光晕成片发亮、枢纽星更大 —— 像一片真实被点亮的星海，不是两个圆盘、也不是均匀散点。
  完整规格另见设计文档《星图布局规则.md》（设计依据，不必喂生成器）。
顶部一行轻量统计：「51% 已亮 · 本周新点亮 8 颗」。

—— 视图 B：思维导图（一棵可展开收起、可编辑的节点树）——
★ 完整生成与数据规则见《思维导图生成规则.md》。形态铁律：必须是节点-连线树（xmind / markmap 那样），不是分栏向导。
  生成器易误做成「选章节 → 关联小节 → 考点细节」三栏 STEP 向导（Miller 分栏）——明确禁止。也不要做成 Kanban 分栏。
  正确层级：科目 → 章节 → 模块 → 主题组（按需存在）→ 知识点。知识点是最小叶节点；只有一个子节点的中间层自动折叠。
  横屏默认根节点「刑法」居中，一级章节向左右两侧平衡展开，减少单向树留白；默认只展开到模块层，知识点按需展开。
亮色背景。节点用左侧色条/圆点表示掌握状态（同语义色）。
【展开 / 收起】每个非叶节点带折叠手柄；顶部提供「收起到章节 / 收起到模块 / 适应屏幕」。一次展开预计超过 80 个节点时，不执行“展开全部”，提示按章节展开。
【紧凑布局】节点用单行紧凑胶囊，状态只常驻色点；叶节点垂直间距 8–12px；收起节点显示隐藏数量。画布可平移缩放，支持聚焦当前分支、搜索定位、面包屑和小地图。
【关系分离】树上的父子实线只表示 contains 分类关系；prerequisite 前置关系可用聚焦态辅助虚线；related 语义关系只驱动星图，不画成父子线。
【可编辑能力】
  - 拖拽节点只修改用户个人分类覆盖层，不改变系统题库归属和星图 cosine 关系。
  - 任意节点旁「＋」新增自己的子节点 / 批注。
  - 双击重命名：仅限用户自建节点；系统知识点正文只读，双击只能加批注、不改正文。
【不可编辑】系统知识点正文、标准答案、系统分类树和星图语义边。个人覆盖层可恢复到系统结构。

—— 视图 C：列表 ——
按同一标准分类树「章节 / 模块 / 主题组（按需）/ 知识点」分组的可折叠列表，每行：状态圆点 + 知识点名 + 状态文字 + 右侧「学习/复习」按钮。
支持按当前筛选 chip 过滤。适合快速查找、扫薄弱项。

【数据】使用下方 JSON（法考·刑法，真实知识结构）。
  ‼ 重要：JSON 里 chapters→sections→concepts 的嵌套**只是数据归属关系，不是画面布局**。星图渲染**不要**照这个嵌套把星按章节分块摆放！每个 concept 自带 `x` / `y`（0~1 归一化画布坐标，已是成簇+暗空形态）和 `deg`（枢纽度=显示边数，越大星越大）。星图**一律按 x,y 画点、按 deg 定大小**，章节字段只用于背景淡字标签与列表/导图分组。
  三层只对应星图语义缩放三档的「标签显示层级」（不是空间分区）：
  chapters[]（章节标签，缩小档显示，浮在星云上不圈区域）→ sections[]（小节，中等档）→ concepts[]（知识点 = 星，放大档可点）。
  每个 concept：status 决定星亮度（星空亮度制：未学暗灰 / 已开始的星更亮，靠亮度深浅区分、不靠色相），deg 决定大小；小节内 concept 全掌握则该簇连线点亮（小庆祝）。
  relations 给出知识点间的 cosine top-3 邻接边（无向、并集，离线预计算固定，是连线的驱动）；仅知识点档显示，任一端掌握后该边点亮。
  另需渲染：一层 300~600 颗极暗背景星尘（不在 JSON 里，生成器自行铺，填满暗空、不可点）。
  星=知识点（一个知识点一颗星，约 240 个）；闪卡只是知识点的展示形式，不是另一层节点。